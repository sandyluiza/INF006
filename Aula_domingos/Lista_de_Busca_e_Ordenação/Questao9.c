// 9 – Refaça as funções de busca sequencial e busca binária assumindo que o vetor possui chaves que podem ocorrer múltiplas vezes no vetor. Neste caso, você deve retornar, em um outro vetor, todas as posições onde a chave foi encontrada. Protótipo: int busca(int vet[], int n, int chave, int posicoes[]); Sua função deve retornar o número de ocorrências da chave no vetor e, para cada uma destas ocorrências, indicar no vetor posicoes[], as posições de vet que possuem a chave.
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
  return 0;
}